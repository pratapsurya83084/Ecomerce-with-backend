import React from 'react'
//context api  is use to share data to multiple component at a time 
//but props can olny share data children component then next children so on...
import { createContext } from 'react'
 const Mycontext=createContext();


export default Mycontext
